# 📘 Інструкція користувача: Smartbook V4

Цей документ допоможе вам зрозуміти, як наповнювати, редагувати та керувати вашою базою знань.

---

## 🏗 Структура проєкту

- **`Данні/Методичка_split_by_sections`**: Тут зберігаються ваші вихідні файли (чернетки). Кожен розділ — це один великий .md файл.
- **`website/docs`**: Це "чистовик", який відображається на сайті. Сюди контент потрапляє після обробки скриптом.
- **`migrate_metadata.py`**: Ваш "двигун". Він бере великі файли з "Данні", розбиває їх на сторінки, додає заголовки та виправляє помилки для сайту.

---

## 🚀 Як додавати новий контент

Ви можете працювати двома способами:

### Спосіб 1: Масова міграція (Рекомендовано для великих оновлень)
1. Відредагуйте або додайте файл у папку `Данні/Методичка_split_by_sections`.
2. Запустіть скрипт командою: `python migrate_metadata.py`.
3. Скрипт автоматично оновить папку `website/docs`.
4. Запушіть зміни в GitHub для оновлення сайту.

### Спосіб 2: Пряме редагування (Для дрібних правок)
Ви можете редагувати файли безпосередньо в `website/docs`. 
> [!WARNING]
> Пам'ятайте: якщо ви запустите скрипт `migrate_metadata.py` пізніше, він перепише файли в `website/docs` контентом з папки `Данні`. 

---

## ✏️ Як редагувати через інтерфейс (GitHub)

Docusaurus сам по собі не має "адмін-панелі", але ви можете використовувати потужність GitHub:

1. **Редагування на сайті**: 
   Незабаром ми додамо кнопку "Edit this page" на кожну сторінку. Вона веде прямо до файлу на GitHub.
2. **Web Editor**: 
   Відкрийте ваш репозиторій на GitHub, знайдіть потрібний файл у `website/docs` і натисніть іконку олівця. Після збереження сайт оновиться сам за 2-3 хвилини.
3. **GitHub.dev (VS Code у браузері)**:
   Натисніть клавішу `.` (крапка) на клавіатурі, знаходячись на головній сторінці репозиторію. Це відкриє повноцінний редактор прямо у вашому браузері.

---

## 🛠 Поради з Markdown

Docusaurus підтримує круті фішки:

- **Підказки (Admonitions)**:
  ```markdown
  :::tip Порада
  Це корисна порада.
  :::
  
  :::danger Увага
  Це критична інформація!
  :::
  ```
- **Діаграми Mermaid**:
  ```mermaid
  graph TD;
      A-->B;
      B-->C;
  ```

---

## 🧹 Як очистити і почати заново
Якщо ви хочете повністю переробити структуру:
1. Видаліть підпапки в `website/docs`.
2. Оновіть файли в `Данні`.
3. Запустіть `python migrate_metadata.py`.
